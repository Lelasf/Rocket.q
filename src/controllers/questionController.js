module.exports = {
  index(req, res) {
    const roomId = req.params.room
    const questionId = red.params.questions
    const action = req.params.action
    const password = req.body.password
  }
}
